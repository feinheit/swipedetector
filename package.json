{
  "name": "swipedetector",
  "version": "1.0.1",
  "description": "Library for detecting swipe gestures from touch and mouse events",
  "main": "dist/swipedetector.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "version": "npm run build && git add -A dist"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/matthiask/swipedetector.git"
  },
  "author": "Matthias Kestenholz",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/matthiask/swipedetector/issues"
  },
  "homepage": "https://github.com/matthiask/swipedetector#readme",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
